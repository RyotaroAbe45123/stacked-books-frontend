import { Box } from "@chakra-ui/react";
import { Card } from "components/Card";

export const Input = () => {
  return (
    <Box>
      <Card>
        <Box h="300px">input</Box>
      </Card>
    </Box>
  );
};
