export type User = {
  userid: number;
  name: string;
};

export type Stack = {
  userid: number;
  isbn: number;
};

export type Book = {
  isbn: number;
  author: string;
  publisher: string;
  title: string;
};
